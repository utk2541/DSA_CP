#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
       ll n;
       cin>>n;
       ll x;
       cin>>x;
       ll a[n];
       for(ll i=0;i<n;i++) cin>>a[i];
       for(ll i=0;i<n;i++){
           
       }
    return 0;
}
